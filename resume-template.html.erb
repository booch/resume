<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">

    <title>Résumé - Craig Buchek</title>

    <meta name="description" content="Craig Buchek's résumé">

    <style type="text/css">
      body { font-family: sans-serif; line-height: 1.3; }
      h1:first-of-type { font-family: sans-serif; font-size: 48px; font-variant: small-caps; font-weight: 500; margin-top: 10px; margin-bottom: -12px; float: left; }
      h1:first-of-type+p { clear: both; float: left; margin: 4px; }
      h1:first-of-type+p+p { float: right; margin: 4px; }
      h2 { font-size: 1.25em; page-break-after: avoid; }
      hr { clear: both; }
      ul { page-break-before: avoid; }
      p:nth-of-type(6) { page-break-after: always; } /* Work-around for `page-break-before: avoid` not working. */
      p:nth-of-type(12) { page-break-after: always; } /* Work-around for `page-break-before: avoid` not working. */
    </style>
  </head>

  <body>
<%= @body %>
  </body>

</html>
